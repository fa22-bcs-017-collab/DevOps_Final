# Application Environment Configuration
NODE_ENV=production
PORT={{ app_port }}
MONGODB_URI=mongodb://{{ hostvars[groups['dbservers'][0]]['ansible_host'] }}:{{ hostvars[groups['dbservers'][0]]['db_port'] }}/taskmanager

# Application Settings
APP_NAME=TaskManager
APP_VERSION=1.0.0

# Logging
LOG_LEVEL=info
